{% extends "base.twig" %}

{% block title %}Create Status Type{% endblock %}

{% block content %}
<div class="d-flex justify-content-between align-items-center mb-4">
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb mb-0">
            <li class="breadcrumb-item"><a href="/">Dashboard</a></li>
            <li class="breadcrumb-item"><a href="/admin/status-types">Status Types</a></li>
            <li class="breadcrumb-item active">Create New</li>
        </ol>
    </nav>

    <a href="/admin/status-types" class="btn btn-outline-secondary">
        <i class="bi bi-arrow-left me-1"></i>Back to List
    </a>
</div>

<div class="row justify-content-center">
    <div class="col-lg-8">
        <div class="card">
            <div class="card-header">
                <h5 class="card-title mb-0">
                    <i class="bi bi-plus-lg me-2"></i>Create New Status Type
                </h5>
            </div>
            <div class="card-body">
                <form method="POST" action="/admin/status-types">
                    <input type="hidden" name="csrf_token" value="{{ csrf_token }}">

                    <div class="mb-3">
                        <label for="statusdesc" class="form-label">Status Description <span class="text-danger">*</span></label>
                        <input type="text" class="form-control" id="statusdesc" name="statusdesc" required>
                        <div class="form-text">Enter a descriptive name for this status type (e.g., "Active", "In Maintenance", "Retired").</div>
                    </div>

                    <div class="d-flex justify-content-between">
                        <a href="/admin/status-types" class="btn btn-secondary">
                            <i class="bi bi-x-lg me-1"></i>Cancel
                        </a>
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-check-lg me-1"></i>Create Status Type
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block scripts %}
<script>
document.addEventListener('DOMContentLoaded', function() {
    document.getElementById('statusdesc').focus();
});
</script>
{% endblock %}